# Age Predictor CI/CD

This is a beginner-level ML project to predict whether a person is an Adult (age ≥ 18) or Minor (age < 18).  

It includes a *CI/CD pipeline using GitHub Actions* that:

- Trains the ML model
- Runs unit tests
- Generates an HTML status page
- Deploys to GitHub Pages automatically